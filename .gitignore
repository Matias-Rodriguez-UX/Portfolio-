# ── Dependencies ──
node_modules/
/.pnp
.pnp.js

# ── Build / output ──
build/
dist/
/coverage

# ── Environment & secrets ──
.env
.env.local
.env.*.local

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS / editor ──
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ── AI-assistant / tooling working dirs (local; not part of the website) ──
# Delete any line below if you DO want to version that tooling or its config.
.claude/
.codex/
.gemini/
.kiro/
.agents/
.impeccable/

# ── Internal strategy doc (kept local). DESIGN.md is intentionally committed. ──
PRODUCT.md
